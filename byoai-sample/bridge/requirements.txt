# Bridge core — required for any deployment
websockets>=12.0
PyJWT>=2.0
truststore
audioop-lts; python_version >= "3.13"

# G.722 wideband codec support.
# Required when running an AI provider with the default G.722 codec on the
# bridge↔Infinity wire. Run `--codec L16` to use narrowband instead and
# skip these dependencies. Echo does not require them.
G722>=1.2.5
numpy>=2.4.0
